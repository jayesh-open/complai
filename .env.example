# Complai Dev Environment
# Copy to .env and adjust as needed

# Database
DATABASE_URL=postgres://complai:complai_dev@localhost:5432/complai_dev?sslmode=disable

# Redis
REDIS_URL=redis://localhost:6379

# AWS / LocalStack — region must be ap-south-1 everywhere (LocalStack, aws-sdk-go-v2, awslocal)
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=ap-south-1
AWS_DEFAULT_REGION=ap-south-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=complai
KEYCLOAK_CLIENT_ID=complai-api

# Temporal
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default

# OpenSearch
OPENSEARCH_URL=http://localhost:9200

# Email (Mailpit)
SMTP_HOST=localhost
SMTP_PORT=1025

# OTel
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Service
SERVICE_PORT=8080
LOG_LEVEL=debug
ENVIRONMENT=development
