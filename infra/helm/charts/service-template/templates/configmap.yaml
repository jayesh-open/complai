apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "complai-service.configMapName" . }}
  namespace: {{ .Values.service.namespace }}
  labels:
    {{- include "complai-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
